# Generated by `wrangler types`. Its exact formatting is asserted by
# `wrangler types --check` (npm run cf-typegen:check), so it must be left
# byte-for-byte as generated. Formatting it here makes the two checks
# contradict each other.
worker-configuration.d.ts
